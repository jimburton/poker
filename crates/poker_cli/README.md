# CLI client for the poker library

See the [docs](../docs/poker_cli/index.html).

The client is very basic. Run it with:

```
$ cargo run --bin poker_cli
```

This launches a game of four players: yourself and three auto players
that use a variety of betting strategies.
